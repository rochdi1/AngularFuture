stand-alone: autonome| eigenständige | قائمة بذاتها
ambient:ambiant | umgebend | الجو المحيط
Runtime:Runtime | Laufzeit | وقت التشغيل
Despite:Malgré | Trotz | على الرغم من
described: décrit | beschrieben | وصف
interface generic: interface générique | Schnittstelle generisch | واجهة عامة
immutable symbole: symbole immuable | unveränderliche symbole | رمز غير قابل للتغيير
several: nombreuses | mehrere | العديد من
flags: drapeaux | Flaggen | الأعلام
targeting: ciblage | Targeting | استهداف
Iteration: action de repeter un processus (for schleife)

//Rest api verbindung
axios ist für client ist ein ajax bibliothek
ExpressJs ist für server 

//
Both for..of and for..in statements iterate over lists; the values iterated on are different though, for..in returns a list of keys on the object being iterated, whereas for..of returns a list of values of the numeric properties of the object being iterated.

let list = [4, 5, 6];

for (let i in list) {
   console.log(i); // "0", "1", "2",
}

for (let i of list) {
   console.log(i); // "4", "5", "6"
}
